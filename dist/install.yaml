apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
    control-plane: controller-manager
  name: vault-k8s-config-operator-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: vaultk8sconfigs.environments.arc.ucl
spec:
  group: environments.arc.ucl
  names:
    kind: VaultK8sConfig
    listKind: VaultK8sConfigList
    plural: vaultk8sconfigs
    singular: vaultk8sconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VaultK8sConfig is the Schema for the vaultk8sconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VaultK8sConfig
            properties:
              auth:
                description: auth defines how the operator authenticates to Vault.
                properties:
                  tokenSecretRef:
                    description: tokenSecretRef references a Kubernetes Secret key
                      that contains a Vault token.
                    properties:
                      key:
                        description: key is the data key in the Secret.
                        minLength: 1
                        type: string
                      name:
                        description: name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - tokenSecretRef
                type: object
              engine:
                description: engine defines the Kubernetes secret engine mount path
                  and desired config.
                properties:
                  clusterCredentialsSecretRef:
                    description: |-
                      clusterCredentialsSecretRef references a Secret that contains JWT and CA cert data.
                      When omitted, the operator provisions a vault-auth ServiceAccount, its long-lived token
                      Secret, a ClusterRole, and a ClusterRoleBinding in the same namespace as the CR and
                      uses that token Secret for JWT and CA cert extraction.
                    properties:
                      caCertKey:
                        default: ca.crt
                        description: caCertKey is the key in the Secret data that
                          stores the Kubernetes CA certificate.
                        type: string
                      jwtKey:
                        default: token
                        description: jwtKey is the key in the Secret data that stores
                          the JWT used by Vault.
                        type: string
                      name:
                        description: name is the Secret name that stores cluster credentials.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the namespace of the Secret. When
                          omitted, defaults to the CR's namespace.
                        type: string
                    required:
                    - name
                    type: object
                  kubernetesHost:
                    description: kubernetesHost is the Kubernetes API server URL used
                      by Vault.
                    pattern: ^https?://.+$
                    type: string
                  mountPath:
                    description: mountPath is the path where the Kubernetes secret
                      engine is enabled in Vault.
                    minLength: 1
                    type: string
                required:
                - kubernetesHost
                - mountPath
                type: object
              vaultAddress:
                description: vaultAddress is the URL of the external Vault instance.
                pattern: ^https?://.+$
                type: string
              vaultNamespace:
                description: vaultNamespace is the Vault Enterprise namespace used
                  for API requests.
                type: string
            required:
            - auth
            - engine
            - vaultAddress
            type: object
          status:
            description: status defines the observed state of VaultK8sConfig
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the VaultK8sConfig resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation reconciled
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-controller-manager
  namespace: vault-k8s-config-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-leader-election-role
  namespace: vault-k8s-config-operator-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: vault-k8s-config-operator-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - namespaces
  - secrets
  - serviceaccounts
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - environments.arc.ucl
  resources:
  - vaultk8sconfigs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - environments.arc.ucl
  resources:
  - vaultk8sconfigs/finalizers
  verbs:
  - update
- apiGroups:
  - environments.arc.ucl
  resources:
  - vaultk8sconfigs/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - clusterrolebindings
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - clusterroles
  verbs:
  - bind
  - create
  - escalate
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-vaultk8sconfig-admin-role
rules:
- apiGroups:
  - environments.arc.ucl.ucl.arc.environments
  resources:
  - vaultk8sconfigs
  verbs:
  - '*'
- apiGroups:
  - environments.arc.ucl.ucl.arc.environments
  resources:
  - vaultk8sconfigs/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-vaultk8sconfig-editor-role
rules:
- apiGroups:
  - environments.arc.ucl.ucl.arc.environments
  resources:
  - vaultk8sconfigs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - environments.arc.ucl.ucl.arc.environments
  resources:
  - vaultk8sconfigs/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-vaultk8sconfig-viewer-role
rules:
- apiGroups:
  - environments.arc.ucl.ucl.arc.environments
  resources:
  - vaultk8sconfigs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - environments.arc.ucl.ucl.arc.environments
  resources:
  - vaultk8sconfigs/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-leader-election-rolebinding
  namespace: vault-k8s-config-operator-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: vault-k8s-config-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: vault-k8s-config-operator-controller-manager
  namespace: vault-k8s-config-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: vault-k8s-config-operator-manager-role
subjects:
- kind: ServiceAccount
  name: vault-k8s-config-operator-controller-manager
  namespace: vault-k8s-config-operator-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-webhook-service
  namespace: vault-k8s-config-operator-system
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app.kubernetes.io/name: vault-k8s-config-operator
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
    control-plane: controller-manager
  name: vault-k8s-config-operator-controller-manager
  namespace: vault-k8s-config-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vault-k8s-config-operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: vault-k8s-config-operator
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: controller:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: webhook-certs
          readOnly: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: vault-k8s-config-operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes:
      - name: webhook-certs
        secret:
          secretName: webhook-server-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-metrics-certs
  namespace: vault-k8s-config-operator-system
spec:
  dnsNames:
  - SERVICE_NAME.SERVICE_NAMESPACE.svc
  - SERVICE_NAME.SERVICE_NAMESPACE.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: vault-k8s-config-operator-selfsigned-issuer
  secretName: metrics-server-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-serving-cert
  namespace: vault-k8s-config-operator-system
spec:
  dnsNames:
  - vault-k8s-config-operator-webhook-service.vault-k8s-config-operator-system.svc
  - vault-k8s-config-operator-webhook-service.vault-k8s-config-operator-system.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: vault-k8s-config-operator-selfsigned-issuer
  secretName: webhook-server-cert
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: vault-k8s-config-operator
  name: vault-k8s-config-operator-selfsigned-issuer
  namespace: vault-k8s-config-operator-system
spec:
  selfSigned: {}
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: vault-k8s-config-operator-system/vault-k8s-config-operator-serving-cert
  name: vault-k8s-config-operator-validating-webhook-configuration
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: vault-k8s-config-operator-webhook-service
      namespace: vault-k8s-config-operator-system
      path: /validate-environments-arc-ucl-v1-vaultk8sconfig
  failurePolicy: Fail
  name: vvaultk8sconfig-v1.kb.io
  rules:
  - apiGroups:
    - environments.arc.ucl
    apiVersions:
    - v1
    operations:
    - CREATE
    - UPDATE
    resources:
    - vaultk8sconfigs
  sideEffects: None
